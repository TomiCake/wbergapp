import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    row: {
        flexDirection: 'row',
    },
    column: {
        flexDirection: 'column',
    },
    container: {
        flex: 1
    },
    periodNumbers: {
        alignItems: 'center',

        flexDirection: 'column',
    }
});
