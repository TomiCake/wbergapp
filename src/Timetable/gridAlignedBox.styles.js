import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        position: 'absolute',
        borderRadius: 2,   
        padding: 0,
        margin: 0,
    },
    contentContainer: {
        padding: 5,
    }
});
